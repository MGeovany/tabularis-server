# Optional rate limiting (e.g. per user_id or IP) can be added here.
# Example: use slowapi or a Redis/in-memory counter with a time window
# (e.g. 10 conversions/day per user) and apply to the convert endpoint.
